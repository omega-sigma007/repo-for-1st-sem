#include <stdio.h>
int main()
{
    int i, j, k, p, sum = 0, n, a[3][3], b[3][3], ab[3][3], m1, n1, m2, n2;
    for (k = 0; k < m1; k++)
        for (j = 0; j < n2; j++)
            for (i = 0; i < m2; i++)
            {
                a[k][i] * b[i][j];
            }
}